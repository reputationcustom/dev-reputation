# Next.js — apenas variáveis públicas (NEXT_PUBLIC_*), lidas pelo browser.
# Segredos (chave secreta do Supabase, tokens da Brandwatch) NUNCA vão aqui —
# ver Princípio técnico 1 em .dev/specs/_index.md. Copie para .env.local
# (gitignored) e preencha com os dados do seu projeto Supabase
# (Settings → API).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
